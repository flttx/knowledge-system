import { LocalAgentTokenManager } from "@/components/settings/local-agent-token-manager";

export const metadata = { title: "Local Agent 设置" };

export default function LocalAgentSettingsPage() {
  return <LocalAgentTokenManager />;
}
